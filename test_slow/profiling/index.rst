SCM profiling harness
=====================

This project exists to profile the eager ``builder-inited`` parse. The lock-up
is reproduced by the ``analyze()`` scan alone, so this index is intentionally
minimal.
